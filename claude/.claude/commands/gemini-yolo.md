---
allowed-tools: Bash(gemini --model gemini-3-pro-preview -y -p:*)
description: Delegate a task to the Gemini CLI
---

Use the Bash tool with the command `gemini --model gemini-3-pro-preview -y -p "{prompt}"` to handle the task described below.

Important:
- Replace `{prompt}` with the user's task description.
- Ensure the text within `{prompt}` is properly escaped for Bash (e.g., escape internal double quotes).

Here is the task:

$ARGUMENTS
